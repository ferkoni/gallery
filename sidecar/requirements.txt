# Pinned exactly. An unpinned torch or open_clip is a silent re-embed waiting to
# happen: a preprocessing change upstream moves the vector space without moving
# model_id, and nothing in the system can detect that.
fastapi==0.141.1
uvicorn==0.52.3
open_clip_torch==3.3.0
pillow==12.3.0
torch==2.13.0
torchvision==0.28.0
